# API v1 Contract

This file is reserved for the shared API contract used by parallel frontend and backend implementation lanes. Task 2 fills the endpoint payloads, enum values, and shared DTO structures.
