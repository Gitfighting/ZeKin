meta {
  name: 04 Login Teacher
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/api/auth/login
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "username": "bruno_teacher",
    "password": "Passw0rd!"
  }
}

tests {
  test("teacher login succeeds", function () {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody().data.access_token).to.be.a("string");
  });

  bru.setEnvVar("teacher_token", res.getBody().data.access_token);
}
