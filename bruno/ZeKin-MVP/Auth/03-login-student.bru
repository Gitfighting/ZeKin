meta {
  name: 03 Login Student
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/api/auth/login
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "username": "bruno_student",
    "password": "Passw0rd!"
  }
}

tests {
  test("student login succeeds", function () {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody().data.access_token).to.be.a("string");
  });

  bru.setEnvVar("student_token", res.getBody().data.access_token);
}
