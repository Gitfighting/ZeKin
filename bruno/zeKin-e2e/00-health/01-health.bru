meta {
  name: 01 健康检查
  type: http
  seq: 1
}

get {
  url: {{base_url}}/health
  body: none
  auth: none
}

tests {
  test("服务可用", function() {
    expect(res.getStatus()).to.equal(200);
    const body = res.getBody();
    expect(body.status).to.equal("ok");
  });
}
