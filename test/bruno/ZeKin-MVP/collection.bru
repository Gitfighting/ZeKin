meta {
  name: ZeKin-MVP
  type: collection
  version: 1
}

docs {
  知勤打卡系统 72 小时 MVP 接口测试集合。
  运行前先启动后端：
  cd backend
  python -m uvicorn app.main:app --reload --port 8000

  然后执行：
  cd test/bruno/ZeKin-MVP
  bru run --env Local
}
