meta {
  name: 00 Register Admin
  type: http
  seq: 0
}

post {
  url: {{baseUrl}}/api/auth/register
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "username": "bruno_admin",
    "password": "Passw0rd!",
    "real_name": "管理员",
    "role": "admin",
    "class_name": "校级",
    "phone": "13900000000"
  }
}

tests {
  test("admin register returns 201 or already exists", function () {
    expect([201, 409]).to.include(res.getStatus());
  });
}
