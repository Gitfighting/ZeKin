<view class="page">
  <view class="title">我的</view>
  <view class="card">
    <view>{{user.real_name || '未登录'}}</view>
    <view class="muted">{{user.role || '教师'}} · {{user.class_name || ''}}</view>
  </view>
  <button bindtap="logout">退出登录</button>
</view>
